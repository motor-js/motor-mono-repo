import React, { useContext } from "react";
import { Col, Row, Tabs } from "antd";
import Auxiliary from "util/Auxiliary";

const Home = () => {
  return (
    <Auxiliary>
      Home
    </Auxiliary>
  );
};

export default Home;
